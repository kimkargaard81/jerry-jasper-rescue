# Jerry & Jasper: Save Miss Marit

A small puzzle adventure built with **Next.js (App Router)** and a **JSON puzzle manifest**.

## Run locally (if you use Codespaces or a dev machine)
```bash
npm install
npm run dev
# open http://localhost:3000
